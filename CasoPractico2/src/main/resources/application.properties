# Server
server.port=78
spring.application.name=PlataformaAcademica

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/plataforma?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=MGsamporu12

# JPA - Para Spring Boot 3.1.5
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# ELIMINA o COMENTA la línea del dialecto - Spring Boot 3.x lo detecta automáticamente
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Layout Dialect
spring.thymeleaf.servlet.content-type=text/html

# Security (si quieres login básico)
# spring.security.user.name=admin
# spring.security.user.password=12345

# Logging
logging.level.org.springframework=WARN
logging.level.com.plataforma=DEBUG